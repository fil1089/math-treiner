
  # Achievement screen design

  This is a code bundle for Achievement screen design. The original project is available at https://www.figma.com/design/8OLMLxdu06spn7xA3v31fx/Achievement-screen-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  